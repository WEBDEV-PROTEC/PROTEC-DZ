<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_journal_form_extend" model="ir.ui.view">
        <field name="name">account.journal.form.extend</field>
        <field name="model">account.journal</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='advanced_settings']" position="after">

                <page string="Impression Facture" name="impression_facture" attrs ="{'invisible':[('type','not in',['sale','purchase'])]}">
                    <group>
                        <group string="Calcule montant" >
                            <div class="text-muted" colspan="2"></div>
                            
                            <field name="equation_montant" />


                        </group>

                                    
                    </group>
                    <div groups="base.group_no_one">
                                    <h3>Comment définir le champ Equation du montant </h3>
                                    <!--<p>Computed fields are defined with the fields
                                    <strong>Dependencies</strong> and <strong>Compute</strong>.</p>
                                    <p>The field <strong>Dependencies</strong> lists the fields that
                                    the current field depends on. It is a comma-separated list of
                                    field names, like <code>name, size</code>. You can also refer to
                                    fields accessible through other relational fields, for instance
                                    <code>partner_id.company_id.name</code>.</p>
                                    <p>The field <strong>Compute</strong> is the Python code to
                                    compute the value of the field on a set of records. The value of
                                    the field must be assigned to each record with a dictionary-like
                                    assignment.</p>-->      
                                    <!--**********-->
                                    <p>Le champ <strong>Equation du montant </strong> est un champ
                                    texte qui vous permet de déclarer un code python qui représente une
                                    équation pour calculer le montant de la facture en lettre.</p>
                                    <p>Cette équation a une syntaxe à respecter comme suit:</p> 
                                    <p><strong> result = invoice.X <strong>"</strong>Opération mathématique<strong>"</strong> invoice.Y </strong></p>
                                    <p>tel que:</p>
                                    <p><strong>result</strong> C'est la nouvelle valeur du montant de la facture</p>
                                    <p><strong>invoice</strong> C'est le dictionnaire des champs de la facture.</p>
                                    <p><strong>Opération mathématique</strong> C'est la somme (+), la multibplication (*), la division (/), ou la sustraction (-) selon le besion du client</p>
                                    <p><strong>X</strong> et <strong>Y</strong> Sont des variables qui pouvont prendre la valeur d'un champ monetaire, flottant ou entier</p>
                                    
                                    <!--*************-->
                                        
                            </div>
                </page>
    
            </xpath>
        </field>
    </record>
    <menuitem id="account_report"
              parent="account.menu_finance"
              name="Rapports"
              groups="base.group_system"
              
              /> <!-- sequence="20" -->
</odoo>