<odoo>
    <data>
        <template id="product_label_a6" inherit_id="product.report_simple_label">
            <xpath expr="//div[1]" position="replace">
            <div style="width: 100%; display: inline-table;">
                <table class="table mb-0">
                    <tr>
                        <th>
                            <div class="row"> 
                                <div class="col-4"> 
                                    <img t-if="product.product_brand_ept_id.logo" t-att-src="'data:image/png;base64,%s' % to_text(product.product_brand_ept_id.logo)" style="max-width: 84px;"  />

                                </div>                             
                                <div class="col-8 pl-2"> 
                                    <strong style="font-size: 10px" t-field="product.name"/>
                                 </div>
                            </div>
                        </th>
                    </tr>
                    <tr>
                        <td style="height: rem;">
                            <div class="row">
                                <div class="col-4 text-center" >
                                    <img t-if="product.image_1024" t-att-src="'data:image/png;base64,%s' % to_text(product.image_1024)" style="width: 100%; margin-bottom: 8px;"/>
                                    <t t-if="product.barcode">
                                        <img alt="Barcode" t-if="len(product.barcode) == 13" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN13', quote_plus(product.barcode or ''), 600, 150)" style="height: 2rem;"/>
                                        <img alt="Barcode" t-elif="len(product.barcode) == 8" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN8', quote_plus(product.barcode or ''), 600, 150)" style="height:2rem;"/>
                                        <img alt="Barcode" t-else="" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('Code128', quote_plus(product.barcode or ''), 600, 150)" style="height: 2rem;"/>
                                        <span style="font-size: 10px" t-field="product.barcode"/>
                                    </t>
                                    
                                </div>
                                <div t-if="product.description_sale" class="col-8 pl-2">                        
                                    <strong style="font-size: 10px">Description</strong> <br/>
                                    <span style="font-size: 10px" t-field="product.description_sale"/>
                                </div>
                            </div>
                        </td>
                    </tr>
                    <tr>
                        <td style="height: 0.5rem;">
                            <div class="row"> 
                                <div class="col-4"> <span style="font-size: 16px" t-field="product.lst_price" t-options="{'widget': 'monetary', 'display_currency': product.company_id.currency_id}"/></div>
                                <div class="col-8">
                                    <span style="padding: 4px 0;" class="tags"> 
                                        <span class="tag" t-foreach="product.dr_tag_ids" t-as="tag" > 
                                            <img class="tag-image" t-if="tag.image" t-attf-src="data:image/gif;base64, #{tag.image}"/>
                                            <a t-else="" t-field="tag.name"/> 
                                        </span>
                                    </span>
                                </div>
                            </div>
                        </td>
                    </tr>
                <!-- <tr t-if="product.description_sale"> 
                        <td class="col offset-md-4 float-right">
                            <span t-field="product.description_sale"/>
                        </td>
                    </tr>
                     -->
<!--                     <tr>
                        <td class="text-center align-middle" style="height: 1rem">
                            <t t-if="product.barcode">
                                <img alt="Barcode" t-if="len(product.barcode) == 13" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN13', quote_plus(product.barcode or ''), 600, 150)" style="width:50%;height::2rem;"/>
                                <img alt="Barcode" t-elif="len(product.barcode) == 8" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('EAN8', quote_plus(product.barcode or ''), 600, 150)" style="width:50%;height::2rem;"/>
                                <img alt="Barcode" t-else="" t-att-src="'/report/barcode/?type=%s&amp;value=%s&amp;width=%s&amp;height=%s' % ('Code128', quote_plus(product.barcode or ''), 600, 150)" style="width:100%;height::2rem;"/>
                            </t>
                        </td>
                    </tr> -->
                </table>
                 
<!--                 <p style="page-break-after: always;" > 
                </p> -->
                
                <style>
                    :root {
                        /* Theme */
                        --primary-color: #1662f3;
                        --light-color: #d0dffc;
                        --dark-color: #283b70;
                        --regular-font: 'Inter', sans-serif;
                    }
                    
                    .top-bg {
                      height: 40px;
                      background-image: url(/product_catalogue/static/img/footer.png);
                      background-repeat: no-repeat;
                      background-size: cover;
                      background-position: top;
                    }
                    td, tbody {
                        background:none
                    }

                    .tags {
                         margin-left: 6px;
                        margin-right: 6px;
                        padding: 0;
                        list-style-type: none;
                        display: flex;
                        flex-directon: row
                        gap: 2rem;
                        flex-wrap: wrap;
                    }

                    .tag {
                        --color: var(--light-color);
                        font-family: var(--regular-font);
                        font-size: .9rem;
                        border-radius: 100rem;
                        /* box-shadow: 0 0 0 1.5px var(--light-color); */
                        background-color: transparent;
                        color: #0095ed;
                        font-weight: bold;
<!--                         transition: background-color 250ms ease, box-shadow 314ms ease; -->
                        width: fit-content;
                        margin-right: 16px
                    }

                    .tag>a {
                        display: inline-block;
                        position: relative;
                        color: var(--dark-color);
                        text-decoration: none;
                        padding: 0.5rem;
                    }

                    .tag-image {
                        max-height: 24px;
                    }

                    .te_product_brand_img {
                        max-width: 130px;
                        margin-right: 15px;
                        margin: 15px 0px 7px;
                    }
                </style>
            </div>
            </xpath>
        </template>
    </data>
</odoo>